[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ['cd tutora-admin-portal && npm ci']

[phases.build]
cmds = ['cd tutora-admin-portal && npm run build']

[start]
cmd = 'cd tutora-admin-portal && npm start'
